- name: Install base packages
  pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - base-devel
    - git
    - vim
    - zsh
    - zsh-completions
    - firefox
    - alacritty
    - dmenu
  tags:
    - install
    - core